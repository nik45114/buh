# 🚀 Подключение /opt/club_assistant к бухгалтерии

## ✅ Готовая конфигурация

**Бухгалтерский сервер (64.188.83.12):**
- API: `http://64.188.83.12:8000`
- API Key: `f632d94a0815ca53930f2168e5cf1a741ce3e67841e5786f696c64b8d8e6895c`
- Telegram бот: @Buh45114_bot
- Статус: ✅ Запущен и протестирован

---

## 📋 Инструкция (3 простых шага)

### Шаг 1: Скопировать файл интеграции

**На сервере Bot (где /opt/club_assistant):**

```bash
# Скопировать с сервера Buh
scp root@64.188.83.12:/opt/accounting-bot/FOR_CLUB_ASSISTANT.py /opt/club_assistant/

# Или если у вас настроен SSH alias "buh":
scp buh:/opt/accounting-bot/FOR_CLUB_ASSISTANT.py /opt/club_assistant/
```

**Альтернатива: скопировать вручную**

Скопируйте содержимое файла [FOR_CLUB_ASSISTANT.py](FOR_CLUB_ASSISTANT.py) и создайте его на сервере Bot:

```bash
cd /opt/club_assistant
nano FOR_CLUB_ASSISTANT.py
# Вставьте содержимое, сохраните (Ctrl+O, Enter, Ctrl+X)
```

---

### Шаг 2: Проверить интеграцию

```bash
cd /opt/club_assistant
python3 FOR_CLUB_ASSISTANT.py
```

**Ожидаемый результат:**

```
🧪 ТЕСТ ИНТЕГРАЦИИ /opt/club_assistant → БУХГАЛТЕРИЯ
============================================================
📡 API Сервер: http://64.188.83.12:8000
🔑 API Key: f632d94a0815ca53930f...
📤 Отправка тестовых данных...
✅ Данные отправлены в бухгалтерию: {...}
============================================================
✅ ТЕСТ ПРОЙДЕН!

📋 Проверьте в Telegram боте @Buh45114_bot:
   /today - транзакции за сегодня
   /balance - текущий баланс
============================================================
```

**Проверить в боте:**

Откройте @Buh45114_bot и отправьте `/today` - должна появиться тестовая транзакция!

---

### Шаг 3: Добавить в обработчик закрытия смены

**Найдите файл обработчика закрытия смены:**

```bash
cd /opt/club_assistant
# Найти файл, где обрабатывается закрытие смены
grep -r "close_shift" --include="*.py"
# или
grep -r "закрыть смену" --include="*.py"
```

Обычно это файл типа:
- `handlers/shift.py`
- `handlers/admin.py`
- `handlers/close_shift.py`

**Добавьте импорт в начало файла:**

```python
from FOR_CLUB_ASSISTANT import send_to_accounting
```

**Добавьте вызов в обработчик закрытия смены:**

```python
@router.message(Command("close_shift"))
async def close_shift_handler(message: Message):
    # ... ваш существующий код расчета смены ...

    cash_total = calculate_cash()      # ваша функция
    card_total = calculate_card()      # ваша функция
    qr_total = calculate_qr()          # ваша функция

    # ДОБАВИТЬ ЭТО:
    await send_to_accounting(
        cash=cash_total,
        cashless=card_total,
        qr=qr_total,
        shift_type="evening",  # или "morning"
        workers_list=["Имя сотрудника"]  # если есть данные
    )

    await message.answer("✅ Смена закрыта и отправлена в бухгалтерию!")
```

---

## 📁 Структура файлов

После выполнения Шага 1:

```
/opt/club_assistant/
├── FOR_CLUB_ASSISTANT.py  ← НОВЫЙ ФАЙЛ
├── handlers/
│   └── shift.py           ← МОДИФИЦИРОВАТЬ (добавить импорт)
├── knowledge.db
├── bot.py
└── ...
```

---

## 🔍 Проверка подключения

### 1. Проверить доступность API с сервера Bot:

```bash
curl http://64.188.83.12:8000/health
```

Должно вернуть:
```json
{"status":"ok","service":"accounting-bot-api"}
```

### 2. Отправить тестовые данные:

```bash
cd /opt/club_assistant
python3 FOR_CLUB_ASSISTANT.py
```

### 3. Проверить в Telegram боте:

Откройте @Buh45114_bot:
```
/today     - транзакции за сегодня
/balance   - текущий баланс
/month     - отчет за месяц
```

---

## 📊 Формат данных для отправки

### Минимальный (только наличка):

```python
await send_to_accounting(cash=15000)
```

### Стандартный (все способы оплаты):

```python
await send_to_accounting(
    cash=15000,
    cashless=8000,
    qr=3500
)
```

### Полный (с расходами и работниками):

```python
await send_to_accounting(
    cash=15000.0,
    cashless=8000.0,
    qr=3500.0,
    shift_type="evening",  # "morning" или "evening"

    # Расходы со смены (опционально)
    expenses_list=[
        {"amount": 500, "description": "Вода 5л x10"},
        {"amount": 1200, "description": "Канцтовары"},
        {"amount": 300, "description": "Чистящие средства"}
    ],

    # Работники смены (опционально)
    workers_list=["Иван Иванов", "Мария Петрова"]
)
```

---

## 🆘 Решение проблем

### Ошибка: "Connection refused"

**Причина:** API сервер недоступен

**Решение:**
```bash
# Проверить доступность
curl http://64.188.83.12:8000/health

# Проверить firewall на сервере Buh
ssh root@64.188.83.12
docker-compose ps  # должен быть accounting_api - Up
```

### Ошибка: "Invalid API Key"

**Причина:** Неправильный API ключ

**Решение:** API ключ уже правильный в файле `FOR_CLUB_ASSISTANT.py`, ничего менять не нужно

### Ошибка: "Timeout"

**Причина:** Firewall блокирует соединение

**Решение на сервере Buh:**
```bash
ssh root@64.188.83.12

# Узнать IP сервера Bot
# Допустим: 192.168.1.50

# Разрешить доступ
sudo ufw allow from 192.168.1.50 to any port 8000

# Или открыть для всех (менее безопасно):
sudo ufw allow 8000/tcp
```

### Ошибка: "ModuleNotFoundError: No module named 'aiohttp'"

**Причина:** Не установлена библиотека aiohttp

**Решение:**
```bash
cd /opt/club_assistant
pip3 install aiohttp
# или
pip install aiohttp
```

---

## 🎯 Что будет происходить после подключения

1. **Bot_Claude закрывает смену** → вызывает `send_to_accounting()`
2. **Данные отправляются по HTTP** → `POST http://64.188.83.12:8000/api/shift-report`
3. **Бухгалтерский API принимает** → создает транзакции автоматически
4. **Данные сохраняются в БД** → доступны в отчетах

**В Telegram боте @Buh45114_bot можно проверить:**
- `/today` - транзакции за сегодня
- `/balance` - текущий баланс
- `/month` - отчет за месяц
- `/payroll` - расчет зарплаты
- `/taxes_quarter` - налоги за квартал
- `/kudir` - Книга учета доходов и расходов

---

## 📝 Пример полного обработчика

```python
from aiogram import Router, F
from aiogram.types import Message
from aiogram.filters import Command
from FOR_CLUB_ASSISTANT import send_to_accounting  # ← ИМПОРТ

router = Router()

@router.message(Command("close_shift"))
async def close_shift(message: Message, session):
    """Закрытие смены с отправкой в бухгалтерию"""

    # Расчет данных смены (ваш существующий код)
    cash_total = await calculate_cash_from_db(session)
    card_total = await calculate_card_from_db(session)
    qr_total = await calculate_qr_from_db(session)

    # Получить расходы (если есть)
    expenses = await get_shift_expenses(session)
    expenses_list = [
        {"amount": e.amount, "description": e.description}
        for e in expenses
    ]

    # Получить работников смены (если есть)
    workers = await get_shift_workers(session)
    workers_list = [w.name for w in workers]

    # Определить тип смены
    from datetime import datetime
    current_hour = datetime.now().hour
    shift_type = "morning" if current_hour < 14 else "evening"

    # ОТПРАВКА В БУХГАЛТЕРИЮ
    success = await send_to_accounting(
        cash=cash_total,
        cashless=card_total,
        qr=qr_total,
        shift_type=shift_type,
        expenses_list=expenses_list,
        workers_list=workers_list
    )

    # Уведомление пользователя
    if success:
        await message.answer(
            f"✅ Смена закрыта!\n\n"
            f"💰 Наличка: {cash_total:,.0f} ₽\n"
            f"💳 Безнал: {card_total:,.0f} ₽\n"
            f"📱 QR: {qr_total:,.0f} ₽\n\n"
            f"📊 Данные отправлены в бухгалтерию"
        )
    else:
        await message.answer(
            "⚠️ Смена закрыта, но не удалось отправить данные в бухгалтерию.\n"
            "Проверьте подключение к серверу бухгалтерии."
        )
```

---

## ✅ Чеклист подключения

- [ ] Скопировать `FOR_CLUB_ASSISTANT.py` в `/opt/club_assistant/`
- [ ] Запустить тест: `python3 FOR_CLUB_ASSISTANT.py`
- [ ] Проверить в Telegram боте @Buh45114_bot: `/today`
- [ ] Найти обработчик закрытия смены в Bot_Claude
- [ ] Добавить импорт: `from FOR_CLUB_ASSISTANT import send_to_accounting`
- [ ] Добавить вызов в обработчик закрытия смены
- [ ] Перезапустить Bot_Claude
- [ ] Протестировать реальное закрытие смены
- [ ] Проверить данные в бухгалтерии: `/today` в @Buh45114_bot

---

## 🚀 Итог

После подключения **каждая смена** из Bot_Claude **автоматически** попадает в бухгалтерию:

✅ Доходы записываются как транзакции
✅ Расходы учитываются отдельно
✅ Данные доступны в отчетах
✅ Можно считать зарплату, налоги, генерировать КУДиР

**Вся бухгалтерия работает автоматически!** 🎉

---

**Нужна помощь?** Telegram: @Buh45114_bot 💬
